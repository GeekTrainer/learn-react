In this module we explored properties (or props), state and events. These represent a core component of React development.

- Add properties to a component
- Add state to an application
- Add event handlers
